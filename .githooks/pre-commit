#!/bin/sh
# Auto-increment the frontend version on every commit, then stage the change
# so it is included in this commit. See scripts/bump-version.js.
#
# Enabled via: git config core.hooksPath .githooks
# (the repo's "prepare" npm script sets this automatically after npm install)

node scripts/bump-version.js || exit 0
git add frontend/package.json

# ═══════════════════════════════════════════════════════════════════════
#  Gemini API Keys - Environment Variables Template
# ═══════════════════════════════════════════════════════════════════════
#
#  Instructions:
#  1. Copy this file to: backend/.env
#  2. Replace the placeholder keys with your actual keys
#  3. Never commit .env to Git (it's already in .gitignore)
#
# ═══════════════════════════════════════════════════════════════════════

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🤖 GEMINI AI API KEYS (4 Keys for Rotation)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Get your keys from: https://aistudio.google.com/api-keys
# Click "Show API key" for each key and paste below
#
# Each key supports: 15 requests/minute
# Total throughput: 60 requests/minute
# Daily quota: 1,000,000 tokens (shared across all keys)

# Key 1 - Get from https://aistudio.google.com/api-keys
GEMINI_API_KEY_1=your-gemini-api-key-1-here

# Key 2
GEMINI_API_KEY_2=your-gemini-api-key-2-here

# Key 3
GEMINI_API_KEY_3=your-gemini-api-key-3-here

# Key 4
GEMINI_API_KEY_4=your-gemini-api-key-4-here

# Enable/disable Gemini AI
GEMINI_ENABLED=true

# Model configuration (optional - defaults are in application.yml)
GEMINI_MODEL=gemini-1.5-flash

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📊 How Rotation Works
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# Request 1 → Key 1 (GB3Q)
# Request 2 → Key 2 (JlQE)
# Request 3 → Key 3 (CmQo)
# Request 4 → Key 4 (XoTc)
# Request 5 → Key 1 (GB3Q) ← Cycles back
# ... and so on
#
# Benefits:
# ✓ 60 requests/minute instead of 15
# ✓ Automatic load balancing
# ✓ Thread-safe rotation
# ✓ No manual intervention needed
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔒 Security Notes
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# ⚠️  NEVER commit this file to Git
# ⚠️  Restrict API keys to your server IP in Google AI Studio
# ⚠️  Rotate keys every 90 days
# ⚠️  Monitor usage in Google AI Studio dashboard
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Other Application Environment Variables (Optional)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Add other environment variables here if needed
# See backend/.env.example for complete list

# Database
# DB_URL=jdbc:postgresql://localhost:5432/shop_management_db
# DB_USERNAME=postgres
# DB_PASSWORD=postgres

# JWT
# JWT_SECRET=mySecretKey123456789012345678901234567890

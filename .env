# Production Environment - Hetzner Server
# PostgreSQL installed on server (NOT in Docker)
# Photos stored in /opt/shop-uploads (NOT in Docker)

# PostgreSQL Configuration
POSTGRES_DB=shop_management_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=SecurePassword@2024  # CHANGE THIS IN PRODUCTION

# Redis Configuration
REDIS_PASSWORD=RedisSecure@2024Pass  # CHANGE THIS IN PRODUCTION

# JWT Configuration
JWT_SECRET=production-jwt-secret-key-2024-very-secure-change-this  # CHANGE THIS

# Application Environment
# NOTE: SPRING_PROFILES_ACTIVE removed - application.yml now has production defaults
NODE_ENV=production

# Build ID
BUILD_ID=production

# Frontend API URL
API_URL=https://api.nammaoorudelivary.in/api

# CORS Configuration
APP_CORS_ALLOWED_ORIGINS=https://nammaoorudelivary.in,https://www.nammaoorudelivary.in
APP_CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
APP_CORS_ALLOWED_HEADERS=*
APP_CORS_ALLOW_CREDENTIALS=true

# Upload Directory Configuration (Windows Local Development)
APP_UPLOAD_DIR=D:/AAWS/nammaooru/uploads
PRODUCT_IMAGES_PATH=D:/AAWS/nammaooru/uploads/products